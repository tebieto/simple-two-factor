import React from "react";
import { HeaderContainer } from "./Header.styles";
const Header = () => {
  return (
    <HeaderContainer>
      <h2>Simple Two Factor</h2>
    </HeaderContainer>
  );
};

export default Header;
